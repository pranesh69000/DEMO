# Frontend (Vite + React)

Quick start:

1. cd frontend
2. npm install
3. npm run dev

This folder is a minimal Vite + React scaffold. Replace the UI with your meeting controls (join meeting, record, upload audio, show transcripts).
